node_modules
node_modules/
out/
dist/
*.log
.DS_Store
.vite/
*.tsbuildinfo

build/icon.png
build/icon.ico
build/icon.icns

# bundled tmux — a ~2MB universal binary, built by scripts/build-tmux.mjs (the source of truth)
# and copied into the app by electron-builder's mac extraResources. Never committed.
/resources/bin/

.playwright-mcp/

# concurrent-session worktrees / local agent state — except the path-scoped rule files, which
# are the deep half of CLAUDE.md and must ship with the repo (a bare `.claude/` would also exclude
# them: git cannot re-include a file whose parent directory is ignored, hence the `/*` form)
.claude/*
!.claude/rules/
.worktrees/

# backend lives in its own repo
/nodebaseserver/

# local session handoff transcripts / app state
.nodeterm/

# internal dev docs (SDD plans/specs, release runbook) — kept local, never tracked
/docs/superpowers/
/docs/RELEASING.md

# local publish helper — never exported to the public repo
/publish.sh
/kanban-launch.mp4
/agents-tour.mp4
/unplug-tour.mp4
/remote-tour.mp4
/kanban-launch-4k.mp4
/dictation-tour.mp4
/canvas-tour.mp4
/hero-tour.mp4
nodeterm.worktrees/
